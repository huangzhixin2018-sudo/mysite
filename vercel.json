{
  "builds": [
    { 
      "src": "app.py", 
      "use": "@vercel/python",
      "config": {
        "requirementsTxt": "requirements_minimal.txt"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "app.py" }
  ]
}
